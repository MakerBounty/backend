const db = require("../db");
const auth = require("../auth");
const debug = require("debug")("core:lib:threads");


async function describeComment(commentId){

}


// get full thread contents
async function detail(bountyThreadId) {

}
